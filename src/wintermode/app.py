"""winter-mode application — main loop and view stack (skeleton).

Step 0 stub: prints the version. The real app arrives in later steps.
"""

from wintermode import __version__


def main() -> None:
    print(f"wintermode {__version__}")
